<div class="section">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="order-summary clearfix">
          <div class="section-title">
            <h3>CATEGORY</h3>
          </div>
          <table class="shopping-cart-table table">
            <thead>
              <tr>
                <th class="text-center">ID</th>
                <th class="text-center">NAME</th>
              </tr>
            </thead>
            <tbody>
              <% @categories.each do |category| %>
                <tr>
                  <td class="text-center"><%= category.id %></td>
                  <td class="text-center"><%= category.name %></td>
                  <div class="text-center">
                    <td><%= link_to fa_icon("eye"), admin_category_path(category) %></td>
                    <td><%= link_to fa_icon("edit"), edit_admin_category_path(category) %></td>
                    <td><%= link_to fa_icon("trash"), [:admin, category], method: :delete, data: { confirm: 'Are you sure?'} %></td>
                  </div>
                </tr>
              <% end %>
            </tbody>
          </table>
          <div class="pull-left">
            <%= link_to 'Add Category', new_admin_category_path, class: "primary-btn" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
